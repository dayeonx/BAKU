# 단위 테스트 결과

- 실행일: 2026-07-30
- 테스트 프레임워크: [Vitest](https://vitest.dev/) 4.1.10 (신규 도입)
- 실행 명령: `npm test` (`vitest run`) / 커버리지: `npm run test:coverage`
- 대상 범위: `src/lib/*` — Phase 단위 화면(페이지) 작업보다 작은, 순수 로직 함수/모듈 단위
- 결과 요약: **7개 파일, 45개 테스트 전부 통과**, 관련 로직 문(statement)·분기(branch)·함수 100% 커버리지

| 테스트 파일 | 테스트 수 | 결과 |
|---|---|---|
| `src/lib/validation.test.ts` | 11 | ✅ 전부 통과 |
| `src/lib/storagePath.test.ts` | 10 | ✅ 전부 통과 (엣지 케이스 1건 발견, 아래 참고) |
| `src/lib/eventTime.test.ts` | 6 | ✅ 전부 통과 |
| `src/lib/semester.test.ts` | 8 | ✅ 전부 통과 |
| `src/lib/departments.test.ts` | 3 | ✅ 전부 통과 |
| `src/lib/eventCategories.test.ts` | 3 | ✅ 전부 통과 |
| `src/lib/temp-password.test.ts` | 4 | ✅ 전부 통과 |
| **합계** | **45** | **✅ 45/45 통과** |

---

## 1. `validation.ts` — 아이디/비밀번호 형식 검증

| # | 테스트 항목 | 검증 내용 | 결과 |
|---|---|---|---|
| 1 | `isValidUsername` 정상 케이스 | 영문+숫자 조합, 4~20자 → `true` | ✅ Pass |
| 2 | `isValidUsername` 최소 길이 미달 | 3자 → `false` | ✅ Pass |
| 3 | `isValidUsername` 최대 길이 초과 | 22자 → `false` | ✅ Pass |
| 4 | `isValidUsername` 한쪽 문자만 포함 | 영문만/숫자만 → `false` | ✅ Pass |
| 5 | `isValidUsername` 특수문자 포함 | 아이디는 특수문자 불가 → `false` | ✅ Pass |
| 6 | `isValidPassword` 정상 케이스 | 영문+숫자 조합, 4~20자 → `true` | ✅ Pass |
| 7 | `isValidPassword` 허용된 특수문자 | `!@#$%^&*_-` 포함 시 → `true` | ✅ Pass |
| 8 | `isValidPassword` 한쪽 문자만 포함 | 영문만/숫자만 → `false` | ✅ Pass |
| 9 | `isValidPassword` 길이 제약 | 3자/22자 → `false` | ✅ Pass |
| 10 | `isValidPassword` 비허용 특수문자 | `()` 등 → `false` | ✅ Pass |
| 11 | `CREDENTIAL_FORMAT_HINT` | 안내 문구가 빈 문자열이 아님 | ✅ Pass |

## 2. `storagePath.ts` — 업로드 파일명 안전화 (`safeFileName`)

> 최근 커밋(`73139cf`, "파일명에 공백/한글 있으면 업로드 실패하던 문제 수정")에 대한 회귀 테스트 포함.

| # | 테스트 항목 | 검증 내용 | 결과 |
|---|---|---|---|
| 1 | 순수 ASCII 파일명 | 변경 없이 그대로 유지 | ✅ Pass |
| 2 | 한글 파일명 | 한글 제거 후 확장자 보존 (`사진.png` → `file.png`) | ✅ Pass |
| 3 | 공백 포함 파일명 | 공백 → `_` 치환 | ✅ Pass |
| 4 | 한글+공백+영문 혼합 | ⚠️ 아래 "발견된 엣지 케이스" 참고 | ✅ Pass (동작 확인 후 기대값 보정) |
| 5 | 허용되지 않는 특수문자 | 파일명 본문에서 제거 | ✅ Pass |
| 6 | 정제 후 빈 파일명 | `file`로 대체 | ✅ Pass |
| 7 | 확장자 없는 파일명 | 원본 그대로 유지 | ✅ Pass |
| 8 | 점(`.`)으로 시작하는 dotfile | 확장자 없는 것으로 처리되어 점만 제거 (`.gitignore` → `gitignore`) | ✅ Pass |
| 9 | 확장자 내 특수문자 | 확장자에서도 특수문자 제거 | ✅ Pass |
| 10 | 대소문자/숫자 | 그대로 보존 | ✅ Pass |

**발견된 엣지 케이스 (버그는 아니나 기록 필요):**
`"행사 사진 event photo.jpeg"` 처럼 한글 뒤에 공백이 이어지는 파일명은 `_event_photo.jpeg`처럼 **맨 앞에 언더스코어(`_`)가 남는** 현상이 있습니다. 한글을 먼저 제거한 뒤 공백을 `_`로 치환하는 순서 때문에, 한글이 있던 자리의 공백이 언더스코어로 바뀌는 것입니다. 실제 업로드에는 문제없는 값이지만(허용 문자만 남음), 파일명 앞에 불필요한 `_`가 붙는 점은 참고용으로 남겨둡니다.

## 3. `eventTime.ts` — 활동 종료 여부 판단 (`isEventOver`)

> 최근 커밋(`13315be`, "앨범도 활동 종료 시각 기준으로 즉시 노출되도록 정밀화")에 대한 회귀 테스트 포함.

| # | 테스트 항목 | 검증 내용 | 결과 |
|---|---|---|---|
| 1 | 미래 날짜, `end_time` 없음 | 아직 종료 안 됨 (`false`) | ✅ Pass |
| 2 | 과거 날짜, `end_date`/`end_time` 없음 | 자정(23:59:59) 기준으로 종료 처리 (`true`) | ✅ Pass |
| 3 | 당일, `end_time` 없음 | 23:59:59 전에는 아직 종료 아님 (`false`) | ✅ Pass |
| 4 | `event_date` 대신 `end_date` 사용 | 시작일이 지났어도 `end_date` 기준으로 판단 | ✅ Pass |
| 5 | `end_time` 분 단위 정밀도 | 지정 시각 전/후로 정확히 분기 | ✅ Pass |
| 6 | 컷오프 직후 1초 | 정확히 초과 시점부터 종료 처리 | ✅ Pass |

## 4. `semester.ts` — 학기 라벨/기간 계산

| # | 테스트 항목 | 검증 내용 | 결과 |
|---|---|---|---|
| 1 | 3월~8월 | 해당 연도 1학기 | ✅ Pass |
| 2 | 9월~12월 | 해당 연도 2학기 | ✅ Pass |
| 3 | 1월~2월 | **전년도** 2학기로 처리 (연도 롤오버) | ✅ Pass |
| 4 | `currentSemesterLabel` 기본값 | 시스템 날짜 기준 계산 | ✅ Pass |
| 5 | `currentSemesterLabel` 명시적 날짜 | 전달된 날짜 기준 계산 | ✅ Pass |
| 6 | `semesterDateRange` 1학기 | `2026-03-01` ~ `2026-09-01`(제외) | ✅ Pass |
| 7 | `semesterDateRange` 2학기 | `2026-09-01` ~ `2027-03-01`(제외), 연도 이월 | ✅ Pass |
| 8 | `semesterLabel` ↔ `semesterDateRange` 왕복 일관성 | 라벨 생성 후 기간 재계산 시 값 일치 | ✅ Pass |

## 5. `departments.ts` — 부서 라벨 조회

| # | 테스트 항목 | 검증 내용 | 결과 |
|---|---|---|---|
| 1 | 정의된 전체 부서 값 | 각 값에 대해 올바른 한글 라벨 반환 | ✅ Pass |
| 2 | 존재하지 않는 값 | 값 자체를 그대로 반환 (fallback) | ✅ Pass |
| 3 | 빈 문자열 | 빈 문자열 반환 | ✅ Pass |

## 6. `eventCategories.ts` — 행사 카테고리 메타데이터 조회

| # | 테스트 항목 | 검증 내용 | 결과 |
|---|---|---|---|
| 1 | 정의된 8개 카테고리 전체 | `categoryMeta`/`categoryLabel`/`categoryColor` 모두 원본과 일치 | ✅ Pass |
| 2 | 존재하지 않는 카테고리 값 | `free`(index 1)로 폴백 | ✅ Pass |
| 3 | 카테고리 값 중복 여부 | 8개 값이 모두 고유함 | ✅ Pass |

## 7. `temp-password.ts` — 최초 계정 자격증명

| # | 테스트 항목 | 검증 내용 | 결과 |
|---|---|---|---|
| 1 | 이미 trim된 학번 | 그대로 반환 | ✅ Pass |
| 2 | 앞뒤 공백 포함 학번 | 공백 제거 후 반환 | ✅ Pass |
| 3 | 공백만 있는 입력 | 빈 문자열 반환 | ✅ Pass |
| 4 | `STUDENT_EMAIL_DOMAIN` 상수 | `@baku.internal` 값 확인 | ✅ Pass |

---

## 커버리지

```
Statements   : 100% (38/38)
Branches     : 100% (23/23)
Functions    : 100% (14/14)
Lines        : 100% (34/34)
```
(대상: `src/lib` 순수 함수 모듈 7종. Supabase 클라이언트, React 페이지/컴포넌트 등 UI·외부 연동 코드는 이번 단위 테스트 범위에서 제외했습니다.)

## 테스트 대상에서 제외한 항목과 이유

- `src/lib/bakingPlace.ts`, `src/lib/bakuAccount.ts`: 로직 없이 고정 문자열만 export하는 상수 모듈이라 단위 테스트 대상에서 제외했습니다.
- `src/app/**/*.tsx`, `src/components/*.tsx`: Supabase 인증/DB 접근과 화면 렌더링이 얽힌 페이지·컴포넌트 단위로, 이번 라운드에서는 순수 로직(`src/lib`)에 한해 테스트를 준비했습니다. 다음 단계로 필요하면 이 페이지들에 대한 통합/컴포넌트 테스트를 추가로 준비하겠습니다.

## 실행 방법

```bash
npm test              # 전체 테스트 1회 실행
npm run test:watch    # 파일 변경 감지하며 실행
npm run test:coverage # 커버리지 리포트 포함 실행
```
